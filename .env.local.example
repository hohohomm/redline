# Supabase (from Project Settings → API in the Supabase dashboard)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Email sending (used from Week 3 onward). Sign up at resend.com.
RESEND_API_KEY=

# Stripe (used from Week 4 onward). From dashboard.stripe.com.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=

# Random string — used to protect the reminder cron from public calls.
CRON_SECRET=

# URL of THIS app (localhost for dev, Vercel URL for prod).
APP_URL=http://localhost:2999
